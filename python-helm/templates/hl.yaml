apiVersion: v1
kind: Service
metadata:
  name: python-hl
  labels:
    app: python
spec:
  type: {{ .Values.python.hl.type }}
  clusterIP: None
  selector:
    app: python
  ports:
	{{- .Values.python.hl.ports | toYaml | nindent 2 -}}
